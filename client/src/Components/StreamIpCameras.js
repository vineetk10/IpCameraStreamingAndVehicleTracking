import React from 'react'
import Button from 'react-bootstrap/Button';

function StreamIpCameras() {
  return (
    <div>
         <Button variant="primary" size="lg">
        Stream IP Cameras
      </Button>
    </div>
  )
}

export default StreamIpCameras